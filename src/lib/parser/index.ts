/**
 * Parser layer entry point.
 *
 * The full implementation is ported from `_prototype/index.html` per the spec
 * in `_prototype/EXTRACTED-SPEC.md` §2. Day 4 work — for now the module exports
 * only the types so other layers can compile against the contract.
 */

export type * from "./types";
